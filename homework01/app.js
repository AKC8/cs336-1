const express = require('express')
const app = express()
const port = 3000

const = [
];
app.get('/', (req, res) => res.sendStatus(404));

iapp.listen(port, () => console.log(`Example app listening on port ${port}!`));

